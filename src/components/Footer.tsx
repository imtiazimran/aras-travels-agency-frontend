import Container from "../utils/Container";

const Footer = () => {
    return (
        <div>
            <Container className="bg-zinc-900 flex justify-center items-center">
                <div>

                </div>
            </Container>
        </div>
    );
};

export default Footer;
